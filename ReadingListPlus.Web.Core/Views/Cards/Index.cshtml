@using ReadingListPlus.Web.Core.Controllers

@model IEnumerable<ReadingListPlus.Web.Core.ViewModels.CardViewModel>

@{
    ViewBag.Title = Model.First().DeckTitle;
}

<h2>@Model.First().DeckTitle</h2>

<a asp-action="@nameof(CardsController.Details)" asp-controller="@DecksController.Name" asp-route-id="@Model.First().DeckID">Back</a>

<table class="table notes-table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.UserFriendlyPosition)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Text)
        </th>
        <th>
        </th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.UserFriendlyPosition)
            </td>
            <td>
                @item.Text
            </td>
            <td>
                <a asp-action="@nameof(CardsController.Details)" asp-route-id="@item.ID">Read</a> |
                <a asp-action="@nameof(CardsController.Edit)" asp-route-id="@item.ID">Edit</a> |
                <a asp-action="@nameof(CardsController.Delete)" asp-route-id="@item.ID">Hide</a>
            </td>
        </tr>
    }

</table>
